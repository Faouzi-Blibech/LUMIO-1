"""
train_classifier.py — Train XGBoost distraction classifier.

Loads the synthetic data from generate_training_data.py,
trains an XGBClassifier with 80/20 train/test split,
prints classification report, and saves the model.

Output: backend/models/distraction_clf.joblib

TODO Day 4: Implement training pipeline with scikit-learn + XGBoost.
"""


def train():
    """Train XGBoost classifier and save to joblib. TODO Day 4."""
    # TODO Day 4: load CSV, train XGBClassifier, save with joblib
    print("train_classifier.py — not yet implemented (Day 4)")


if __name__ == "__main__":
    train()
